# 서버
SERVER_PORT=8080

# PostgreSQL (기본 DB, 로컬은 docker-compose up -d postgres 후 사용)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=trenda
POSTGRES_USER=trenda
POSTGRES_PASSWORD=trenda_password

# 외부 PostgreSQL (Render, Neon 등) 사용 시
# 대시보드에서 복사한 연결 문자열을 그대로 넣으면 됨. 비밀번호 포함이므로 .env는 커밋하지 말 것.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require

